package lesson03_control_flow;

/*
 * 03 — Control Flow
 *
 * Goal: branch and loop.
 *
 * Rules:
 *   - if / else if / else for branching.
 *   - switch expression (Java 14+) uses arrow syntax: case X -> ...
 *   - for, while, do-while for loops.
 *   - `break` exits a loop, `continue` skips to the next iteration.
 *
 * Tasks:
 *   1. Given int score = 75, print a grade:
 *        >=80 A, >=70 B, >=60 C, >=50 D, else F.
 *   2. Re-do task 1 using a switch expression on score / 10.
 *   3. Print numbers 1..10 with a for loop, but skip multiples of 3.
 *   4. Use a while loop to find the smallest power of 2 greater than 1000.
 *   5. Print a 3x3 multiplication grid using nested for loops.
 *
 * Expected output:
 *   grade=B
 *   grade(switch)=B
 *   1 2 4 5 7 8 10
 *   first power of 2 > 1000 = 1024
 *   1 2 3
 *   2 4 6
 *   3 6 9
 *
 * Run:
 *   cd src
 *   javac lesson03_control_flow/Main.java
 *   java lesson03_control_flow.Main
 */
public class Main {
  public static void main(String[] args) {
    // your code here
  }
}
