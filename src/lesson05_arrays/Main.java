package lesson05_arrays;

/*
 * 05 — Arrays
 *
 * Goal: work with fixed-size indexed sequences.
 *
 * Rules:
 *   - Arrays have a fixed length set at creation: int[] a = new int[5];
 *   - Access: a[0]; length: a.length (a field, not a method).
 *   - 2D arrays are arrays of arrays: int[][] grid = new int[3][3];
 *   - java.util.Arrays has helpers: sort, toString, copyOf, fill.
 *
 * Tasks:
 *   1. Create int[] nums = {5, 2, 8, 1, 9}. Print sum, min, max.
 *   2. Reverse `nums` in place (swap from both ends). Print the result.
 *   3. Make a sorted copy with Arrays.sort(nums.clone()) and print with
 *      Arrays.toString.
 *   4. Build int[][] grid = {{1,2,3},{4,5,6},{7,8,9}} and print as a grid.
 *   5. Make String[] words = {"java","is","fun"} and print
 *      String.join(" ", words).
 *
 * Expected output:
 *   sum=25 min=1 max=9
 *   reversed=[9, 1, 8, 2, 5]
 *   sorted=[1, 2, 5, 8, 9]
 *   1 2 3
 *   4 5 6
 *   7 8 9
 *   java is fun
 *
 * Run:
 *   cd src
 *   javac lesson05_arrays/Main.java
 *   java lesson05_arrays.Main
 */
public class Main {
  public static void main(String[] args) {
    // your code here
  }
}
