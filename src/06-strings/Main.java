/*
 * 06 — Strings
 *
 * Goal: manipulate text.
 *
 * Rules:
 *   - String is IMMUTABLE — every "modification" returns a new String.
 *   - Use `.equals(...)` for value comparison; `==` compares references.
 *   - Use StringBuilder for repeated concatenation in loops.
 *   - Text blocks use triple double-quotes: """ ... """.
 *
 * Tasks:
 *   1. Given String s = "Hello, Java", print:
 *        length, upper case, lower case, contains("Java"),
 *        replace "Java" with "World", substring from index 7.
 *   2. Split "a,b,c" by "," and print each part on a new line.
 *   3. Check if "racecar" is a palindrome (case-insensitive).
 *   4. Build a multi-line string with a text block and print it.
 *   5. Compare "hi" and new String("hi") with `==` (false) and `.equals` (true).
 *      Print both results.
 *
 * Expected output:
 *   length=11
 *   upper=HELLO, JAVA
 *   lower=hello, java
 *   contains Java=true
 *   replace=Hello, World
 *   substring=Java
 *   a
 *   b
 *   c
 *   palindrome=true
 *   line 1
 *   line 2
 *   ==:false equals:true
 *
 * Run:
 *   java src/06-strings/Main.java
 */
public class Main {
  public static void main(String[] args) {
    // your code here
  }
}
